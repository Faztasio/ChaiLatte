define([
  "./object"
], function(object) {
  
  cl = function(means) {
    return cl.handle(means)
  }
  
  objectId = function(id) {
    
  }
  
  tag = function(obj) {
    tag.handle(obj)
  }
  
  cl.prototype = {
  
    handle: function(means) {
      if (means[0] == "#") {
        
      }
    }
    
  }
  
  tag.prototype = {
  }
});
